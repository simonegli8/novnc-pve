#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export DEB_BUILD_HARDENING=1

override_dh_install:
	cp vnc.html index.html.tpl
	dh_install

%:
	dh ${@} --with quilt
